import { motion, AnimatePresence } from 'framer-motion'; import { X } from 'lucide-react'; import styles from './styles.module.css';
export default function Modal({ open, title, children, onClose }){return <AnimatePresence>{open&&<motion.div className={styles.overlay} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={onClose}><motion.div className={styles.modal} initial={{scale:.94,y:12}} animate={{scale:1,y:0}} exit={{scale:.94,y:12}} onClick={(event)=>event.stopPropagation()}><header><h3>{title}</h3><button onClick={onClose}><X size={18}/></button></header>{children}</motion.div></motion.div>}</AnimatePresence>}
