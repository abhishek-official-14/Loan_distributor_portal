import { motion } from 'framer-motion'; import { CheckCircle2 } from 'lucide-react'; import Button from '../Button'; import styles from './styles.module.css';
export default function PlanCard({ plan, onActivate, active }) { return <motion.article className={`${styles.card} ${plan.popular ? styles.featured : ''}`} whileHover={{y:-8}} transition={{duration:.25}}>{plan.popular&&<span className={styles.badge}>Most Popular</span>}<p>{plan.description}</p><h3>{plan.name}</h3><div className={styles.price}><strong>{plan.price}</strong><span>/ month</span></div><ul>{plan.features.map((feature)=><li key={feature}><CheckCircle2 size={17}/>{feature}</li>)}</ul><Button onClick={()=>onActivate(plan)} variant={active?'secondary':'primary'}>{active?'Current Plan':'Activate Plan'}</Button></motion.article> }
