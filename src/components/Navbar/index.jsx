import { Bell, Menu, Moon, Search, Sun } from 'lucide-react'; import { useTheme } from '../../hooks/useTheme'; import { useAuth } from '../../hooks/useAuth'; import styles from './styles.module.css';
export default function Navbar({onMenu}){const {theme,toggleTheme}=useTheme();const {user}=useAuth();return <header className={styles.nav}><button className={styles.menu} onClick={onMenu}><Menu size={21}/></button><label className={styles.search}><Search size={17}/><input placeholder="Search loans, customers..."/></label><div className={styles.actions}><button onClick={toggleTheme} title="Toggle theme">{theme==='light'?<Moon size={18}/>:<Sun size={18}/>}</button><button className={styles.bell}><Bell size={18}/><i/></button><div className={styles.user}><span>{user?.fullName?.split(' ').map((name)=>name[0]).join('').slice(0,2)||'RM'}</span><div><strong>{user?.fullName||'Riya Malhotra'}</strong><small>Distributor</small></div></div></div></header>}
