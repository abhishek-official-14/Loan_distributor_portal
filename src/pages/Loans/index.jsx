import { motion } from 'framer-motion'; import { Download, Filter, Plus, Search } from 'lucide-react'; import Button from '../../components/Button'; import { Badge } from '../Dashboard'; import { loans } from '../../data'; import styles from './styles.module.css';
export default function Loans(){return <motion.section initial={{opacity:0}} animate={{opacity:1}}><header className={styles.head}><div><h1>Loan Applications</h1><p>Manage and track all your customer loan applications.</p></div><Button><Plus size={16}/> New Application</Button></header><div className={styles.filters}><label><Search size={16}/><input placeholder="Search customer or loan ID"/></label><Button variant="secondary"><Filter size={15}/> All Status</Button><Button variant="secondary"><Download size={15}/> Export</Button></div><article className={styles.card}><div className={styles.scroll}><table><thead><tr><th>Loan ID</th><th>Customer</th><th>Loan Type</th><th>Amount</th><th>Status</th><th>Applied On</th></tr></thead><tbody>{loans.map((row)=><tr key={row.id}><td>{row.id}</td><td>{row.customer}</td><td>{row.type}</td><td>{row.amount}</td><td><Badge status={row.status}/></td><td>{row.date}</td></tr>)}</tbody></table></div></article></motion.section>}
