import { motion } from 'framer-motion'; import { toast } from 'react-toastify'; import { useNavigate } from 'react-router-dom'; import PlanCard from '../../components/PlanCard'; import { plans } from '../../data'; import styles from './styles.module.css';
export default function Subscription(){const navigate=useNavigate();const active=localStorage.getItem('loanly-plan');const activate=(plan)=>{localStorage.setItem('loanly-plan',plan.name);toast.success(`${plan.name} plan activated successfully!`);navigate('/dashboard')};return <motion.section initial={{opacity:0}} animate={{opacity:1}}><header className={styles.head}><p>FIND YOUR PERFECT PLAN</p><h1>Choose a plan that grows with you</h1><span>Unlock powerful tools, better commissions, and quality leads for your distribution business.</span></header><div className={styles.plans}>{plans.map((plan)=><PlanCard key={plan.name} plan={plan} active={active===plan.name} onActivate={activate}/>)}</div><small className={styles.note}>✓ Secure billing &nbsp; ✓ Cancel anytime &nbsp; ✓ Upgrade whenever you need</small></motion.section>}
