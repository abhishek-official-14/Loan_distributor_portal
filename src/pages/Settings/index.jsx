import { motion } from 'framer-motion'; import { Bell, LockKeyhole, Moon, ShieldCheck } from 'lucide-react'; import { useTheme } from '../../hooks/useTheme'; import styles from './styles.module.css';
const options=[['Email notifications','Loan updates and monthly statement alerts',Bell],['Two-factor authentication','Add an extra security layer to your account',ShieldCheck],['Biometric verification','Use biometric confirmation for payouts',LockKeyhole]];
export default function Settings(){const{theme,toggleTheme}=useTheme();return <motion.section initial={{opacity:0}} animate={{opacity:1}}><header><h1>Settings</h1><p>Manage your preferences and account security.</p></header><article className={styles.card}><h3>Preferences</h3><div className={styles.row}><Moon/><div><strong>Dark mode</strong><small>Switch between light and dark appearance</small></div><button className={`${styles.toggle} ${theme==='dark'?styles.on:''}`} onClick={toggleTheme}><i/></button></div>{options.map(([name,description,Icon],index)=><div className={styles.row} key={name}><Icon/><div><strong>{name}</strong><small>{description}</small></div><button className={`${styles.toggle} ${index<2?styles.on:''}`}><i/></button></div>)}</article></motion.section>}
